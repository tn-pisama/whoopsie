{
  "name": "@whoopsie/ingest",
  "version": "0.0.0",
  "private": true,
  "description": "OTEL span ingest endpoint for whoopsie.dev. Vercel Functions on Fluid Compute.",
  "type": "module",
  "scripts": {
    "build": "echo 'apps/ingest is a placeholder; routes live in apps/web/app/api for v0' && exit 0"
  }
}
