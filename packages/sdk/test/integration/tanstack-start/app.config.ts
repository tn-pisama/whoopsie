import { defineConfig } from "@tanstack/react-start/config";

// Minimal TanStack Start config. The integration goal is to exercise the
// framework's server-route convention with @whoopsie/sdk's observe() helper,
// not to ship a real product, so we keep config near defaults.
export default defineConfig({});
